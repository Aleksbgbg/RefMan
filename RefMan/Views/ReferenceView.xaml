<UserControl x:Class="RefMan.Views.ReferenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Caliburn="http://www.caliburnproject.org"
			 xmlns:ViewModels="clr-namespace:RefMan.ViewModels"
			 xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance ViewModels:ReferenceViewModel}">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="1"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
            <Image Source="{Binding Reference.ImageUrl}"
                   Height="25" Width="25"
                   VerticalAlignment="Center" Margin="2.5, 0"/>

            <TextBlock Text="{Binding Reference.PageTitle}"
                       FontSize="16" FontWeight="DemiBold"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button ToolTip="Delete" Margin="0, 5"
                    Caliburn:Message.Attach="Delete">
                <Image Source="{StaticResource Delete}"/>
            </Button>

            <ToggleButton IsChecked="{Binding IsEditing}"
                          ToolTip="Toggle Edit" Margin="0, 5">
                <Image Source="{StaticResource Edit}"/>
            </ToggleButton>
        </StackPanel>

        <ContentControl Grid.Column="2" Grid.Row="2">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <xctk:PropertyGrid SelectedObject="{Binding Reference, Mode=OneTime}"
                                                       AutoGenerateProperties="False"
                                                       ShowDescriptionByTooltip="True"
                                                       ShowSearchBox="False"
                                                       ShowSortOptions="False"
                                                       ShowSummary="False"
                                                       ShowTitle="False">
                                        <xctk:PropertyGrid.PropertyDefinitions>
                                            <xctk:PropertyDefinition TargetProperties="Url"/>
                                            <xctk:PropertyDefinition TargetProperties="PageTitle"/>
                                            <xctk:PropertyDefinition TargetProperties="WebsiteName"/>
                                            <xctk:PropertyDefinition TargetProperties="AccessDate"/>
                                            <xctk:PropertyDefinition TargetProperties="YearPublished"/>
                                        </xctk:PropertyGrid.PropertyDefinitions>
                                    </xctk:PropertyGrid>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                            <Setter Property="Content">
                                <Setter.Value>
                                    <StackPanel>
                                        <TextBlock Text="In-text:" FontWeight="DemiBold"/>
                                        <TextBox
                                            Text="{Binding Reference, Mode=OneTime, StringFormat={}{0:IT}}"
                                            Style="{StaticResource TextBox.SelectableTextBlock}"/>

                                        <TextBlock Text="Bibliography:" FontWeight="DemiBold" VerticalAlignment="Top"/>
                                        <TextBox
                                            Text="{Binding Reference, Mode=OneTime, StringFormat={}{0:IB}}"
                                            Style="{StaticResource TextBox.SelectableTextBlock}"/>
                                    </StackPanel>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</UserControl>